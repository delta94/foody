module.exports = {
  definition: `
    type Recipe {
      uri: String!
      label: String!
      source: String!
      url: String!
      image: String!
    }
  `,
  query: `
    recipes(ingredients: String!): [Recipe]
  `,
  resolver: {
    Query: {
      recipes: {
        description: 'Return a single person',
        resolver: 'Recipes.recipes'
      }
    }
  }
};
